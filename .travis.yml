matrix:
  include:
    - language: python
      python:
        - "3.7"
      
      # Change into python directory
      before_script:
        - cd backend/server/chess/chess_tests

      # command to install dependencies
      install:
        - pip install -r requirements.txt
      # command to run tests
      script:
      #  - pytest
         - python UnitTests.py